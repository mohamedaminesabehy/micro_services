const express = require('express');
const router = express.Router();

// Remove or comment out the default route
// router.get('/', function(req, res, next) {
//   res.render('index', { title: 'Welcome to Express' });
// });

module.exports = router;